{
  "name": "ts-ast-lab",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "commit": "git-cz"
  },
  "private": true,
  "lint-staged": {
    "*.{ts}": [
      "eslint --max-warnings=0 --fix",
      "git add"
    ]
  },
  "gitHooks": {
    "pre-commit": "lint-staged"
  },
  "devDependencies": {
    "git-cz": "^4.9.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.1.1"
  }
}
